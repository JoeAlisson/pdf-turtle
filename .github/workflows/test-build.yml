name: build and test
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**/README.md'
      - '**/README_DEV.md'
      - '.vscode/**'
      - '.dockerignore'
      - '.gitignore'
    tags-ignore:
      - 'v*'

jobs:
  test-and-build:
    uses: ./.github/workflows/tests.yml
